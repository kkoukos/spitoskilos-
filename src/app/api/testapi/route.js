export async function GET(req) {
  // const temp = await fetch("/api/testapi");

  //     temp.json().then((value) => {
  //       console.log(value.text);
  //     });
  return Response.json({ text: "test3" });
}
